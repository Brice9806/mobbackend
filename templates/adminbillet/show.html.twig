{% extends 'base.html.twig' %}

{% block title %}Billet{% endblock %}

{% block body %}
    <h1>Billet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billet.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ billet.prix }}</td>
            </tr>
            <tr>
                <th>Paye</th>
                <td>{{ billet.paye ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>NomPrenomPassager</th>
                <td>{{ billet.nomPrenomPassager }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adminbillet_index') }}">back to list</a>

    <a href="{{ path('app_adminbillet_edit', {'id': billet.id}) }}">edit</a>

    {{ include('adminbillet/_delete_form.html.twig') }}
{% endblock %}
