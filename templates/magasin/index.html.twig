{% extends 'base.html.twig' %}

{% block title %}Magasin index{% endblock %}

{% block body %}
    <style>
        /* Styles pour la page */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* Styles pour l'en-tête */
        h1 {
            font-size: 36px;
            color: #333;
            margin: 20px 0;
            text-align: center;
            text-transform: uppercase;
        }

        /* Styles pour la table */
        table {
            width: 90%;
            max-width: 1500px;
            background-color: #fff;
            border-collapse: collapse;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
            margin-top: 20px;
        }

        table th, table td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        table th {
            background-color: #27A462;
            color: #fff;
        }

        table td {
            background-color: #f9f9f9;
        }

        /* Styles pour les boutons */
        .btn {
            display: inline-block;
            padding: 6px 12px;
            margin-right: 8px;
            border: none;
            background-color: #27A462;
            color: #fff;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .btn:hover {
            background-color: #0056b3;
        }

        a {
        text-decoration: none;
        padding: 8px 16px;
        background-color: #27A462;
        color: #fff;
        border-radius: 4px;
        margin: 10px;
        display: inline-block;
    }

    a:hover {
        background-color: #0056b3;
    }

        /* Styles pour les animations */
        .animated {
            animation-duration: 1s;
        }

        .fadeIn {
            animation-name: fadeIn;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>

    <h1 class="animated fadeIn">Magasin index</h1>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TypeMagasin</th>
                <th>NomMagasin</th>
                <th>NumeroMagasin</th>
                <th>LiensMaps</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for magasin in magasins %}
            <tr>
                <td>{{ magasin.id }}</td>
                <td>{{ magasin.typeMagasin }}</td>
                <td>{{ magasin.nomMagasin }}</td>
                <td>{{ magasin.numeroMagasin }}</td>
                <td>{{ magasin.liensMaps }}</td>
                <td>
                    <a href="{{ path('app_magasin_show', {'id': magasin.id}) }}"class="btn">show</a>
                    <a href="{{ path('app_magasin_edit', {'id': magasin.id}) }}"class="btn">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_magasin_new') }}"class="btn">Create new</a>
{% endblock %}
