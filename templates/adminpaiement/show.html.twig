{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>ModePaiement</th>
                <td>{{ paiement.modePaiement }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paiement.montant }}</td>
            </tr>
            <tr>
                <th>DatePaiement</th>
                <td>{{ paiement.datePaiement ? paiement.datePaiement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>HeurePaiement</th>
                <td>{{ paiement.heurePaiement ? paiement.heurePaiement|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adminpaiement_index') }}">back to list</a>

    <a href="{{ path('app_adminpaiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('adminpaiement/_delete_form.html.twig') }}
{% endblock %}
